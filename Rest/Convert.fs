namespace Rest

module Convert = ()
  // let key convert revert =
  //   RestResource.mapHandler (fun handler ->
  //     RestRequest.mapKey convert
  //     >> handler
  //     >> RestResult.mapKey revert
  //   )

  // let entity convert convertPatch convertQuery revert revertPatch revertQuery =
  //   RestResource.mapHandler (fun handler ->
  //     RestRequest.mapEntity convert convertPatch convertQuery
  //     >> handler
  //     >> RestResult.mapEntity revert revertPatch revertQuery
  //   )
